[project]
name = "project"
version = "1.0.0"
description = "A new aeon project - empty template"

[systemDependencies]
# System libraries to link against
# Format: library_name = { package = "package_name", libs = ["lib1", "lib2"], includes = ["/path/to/includes"] }
# Or simple format: library_name = ["lib1", "lib2"]
# openssl = { package = "openssl", libs = ["ssl", "crypto"], includes = ["/usr/local/opt/openssl/include"] }
# boost = ["boost_system", "boost_filesystem"]
# sqlite3 = ["sqlite3"]

